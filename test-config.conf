# Test configuration for MaxCltvDelta
network=regtest
datadir=./test-data
maxcltvdelta=200
debuglevel=debug

# Server configuration (pointing to a test server or mock)
[server]
host=localhost:11010
notls=true 