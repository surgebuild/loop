# LND Configuration for your existing custom signet setup
# Your LND is already running on localhost:10009

# LND Host and Port (your existing LND)
LND_HOST=localhost:10009

# Path to your LND directory (update this to your actual LND path)
LND_DIR=/path/to/your/.lnd

# The docker-compose will automatically find:
# - Admin macaroon at: ${LND_DIR}/data/chain/bitcoin/signet/admin.macaroon  
# - TLS certificate at: ${LND_DIR}/tls.cert

# Your Bitcoin signet is already running on:
# - RPC: localhost:38332 (bitcoin:bitcoin)
# - ZMQ: localhost:28332, localhost:28333 