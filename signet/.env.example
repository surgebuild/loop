# External LND Configuration
# Copy this file to .env and configure your LND connection details

# LND Host and Port
LND_HOST=localhost:10009

# Path to LND admin macaroon 
LND_MACAROON_PATH=/path/to/your/lnd/data/chain/bitcoin/signet/admin.macaroon

# Path to LND TLS certificate
LND_TLS_PATH=/path/to/your/lnd/tls.cert

# Optional: External LND directory to mount (if using Docker volume)
EXTERNAL_LND_DIR=/path/to/your/lnd 